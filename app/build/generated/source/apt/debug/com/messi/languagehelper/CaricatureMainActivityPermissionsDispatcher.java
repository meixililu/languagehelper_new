// This file was generated by PermissionsDispatcher. Do not modify!
package com.messi.languagehelper;

import android.support.v4.app.ActivityCompat;
import java.lang.Override;
import java.lang.String;
import java.lang.ref.WeakReference;
import permissions.dispatcher.PermissionRequest;
import permissions.dispatcher.PermissionUtils;

final class CaricatureMainActivityPermissionsDispatcher {
  private static final int REQUEST_SHOWPERMISSION = 6;

  private static final String[] PERMISSION_SHOWPERMISSION = new String[] {"android.permission.READ_PHONE_STATE","android.permission.READ_EXTERNAL_STORAGE","android.permission.WRITE_EXTERNAL_STORAGE","android.permission.ACCESS_FINE_LOCATION"};

  private CaricatureMainActivityPermissionsDispatcher() {
  }

  static void showPermissionWithPermissionCheck(CaricatureMainActivity target) {
    if (PermissionUtils.hasSelfPermissions(target, PERMISSION_SHOWPERMISSION)) {
      target.showPermission();
    } else {
      if (PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_SHOWPERMISSION)) {
        target.onShowRationale(new CaricatureMainActivityShowPermissionPermissionRequest(target));
      } else {
        ActivityCompat.requestPermissions(target, PERMISSION_SHOWPERMISSION, REQUEST_SHOWPERMISSION);
      }
    }
  }

  static void onRequestPermissionsResult(CaricatureMainActivity target, int requestCode,
      int[] grantResults) {
    switch (requestCode) {
      case REQUEST_SHOWPERMISSION:
      if (PermissionUtils.verifyPermissions(grantResults)) {
        target.showPermission();
      } else {
        target.onPerDenied();
      }
      break;
      default:
      break;
    }
  }

  private static final class CaricatureMainActivityShowPermissionPermissionRequest implements PermissionRequest {
    private final WeakReference<CaricatureMainActivity> weakTarget;

    private CaricatureMainActivityShowPermissionPermissionRequest(CaricatureMainActivity target) {
      this.weakTarget = new WeakReference<CaricatureMainActivity>(target);
    }

    @Override
    public void proceed() {
      CaricatureMainActivity target = weakTarget.get();
      if (target == null) return;
      ActivityCompat.requestPermissions(target, PERMISSION_SHOWPERMISSION, REQUEST_SHOWPERMISSION);
    }

    @Override
    public void cancel() {
      CaricatureMainActivity target = weakTarget.get();
      if (target == null) return;
      target.onPerDenied();
    }
  }
}
